//Tipagem de funções? Parâmetos e Retorno

// Tipagem do 1º parâmetro, 2º parâmetro como também a resposta/retorno da função.
function subitracao(a: number, b: number): number {
  return a - b;
}

function soma(a: number, b: number): number {
  return a + b;
}

console.log(soma(5, 5));
